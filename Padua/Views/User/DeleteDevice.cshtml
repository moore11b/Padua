@model Padua.Models.Entities.Devices

@{
    ViewData["Title"] = "DeleteDevice";
}

<h2>DeleteDevice</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>DeviceListVM</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.DevName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.DevName)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.DevMAC)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.DevMAC)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input asp-for="Id" type="hidden" value="@Model.Id" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-controller="User" asp-action="Devices">Back to Devices</a>
    </form>
</div>
